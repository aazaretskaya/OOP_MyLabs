#include <iostream>
// #include "CustomMemoryResource.h"
// #include "Stack.h"
// #include "ComplexType.h"

int main() {
    // CustomMemoryResource mr;
    // Stack<int> intStack(&mr);
    // Stack<ComplexType> complexStack(&mr);

    // intStack.push(1);
    // intStack.push(2);
    // intStack.push(3);

    // complexStack.push({1, 2.5, "Hello"});
    // complexStack.push({4, 5.6, "World"});

    // std::cout << "Int stack top: " << intStack.top() << std::endl;
    // std::cout << "Complex stack top: " << complexStack.top().c << std::endl;

    // intStack.pop();
    // complexStack.pop();

    // std::cout << "Int stack top after pop: " << intStack.top() << std::endl;
    // std::cout << "Complex stack top after pop: " << complexStack.top().c << std::endl;

    // return 0;
}